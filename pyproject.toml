[project]
name = "numbersai-ver13"
version = "0.1.0"
description = "Modular lottery prediction project"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
